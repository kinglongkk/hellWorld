define(['app/route/StringRouter'
        ], function (StringRouter) {

    return Class.extend({

        /**
         * 构造方法
         */
        init : function () {
            StringRouter.init();
        }
    });
});